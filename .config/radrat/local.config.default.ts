// ! Do not remove this file.
import { RadratConfig } from '@radrat-types/config';

const config: RadratConfig = {
    currentWorkingApp: `core`,
    externalHost: `192.168.1.11`,
    platform: `android`,
};

export default config;
